import { NextResponse } from "next/server";
import { z } from "zod";
import { canWrite } from "@/backend/lib/context";
import { prisma } from "@/backend/lib/prisma";
import { audit } from "@/backend/lib/audit";
import { decimalToNumber, jsonError, requireApiContext } from "@/backend/lib/http";
const schema = z.object({ accountId: z.string(), openingBalance: z.coerce.number().nonnegative(), notes: z.string().max(1000).nullable().optional() });
export async function GET() { const auth = await requireApiContext(); if ("error" in auth) return auth.error; return NextResponse.json(decimalToNumber(await prisma.cashSession.findMany({ where: { restaurantId: auth.ctx.restaurant.id }, include: { account: true, openedBy: { select: { name: true } }, closedBy: { select: { name: true } } }, orderBy: { openedAt: "desc" } }))); }
export async function POST(request: Request) { const auth = await requireApiContext(); if ("error" in auth) return auth.error; if (!canWrite(auth.ctx.membership.role)) return jsonError("Sem permissão", 403); const p = schema.safeParse(await request.json().catch(() => null)); if (!p.success) return jsonError("Dados inválidos"); const account = await prisma.account.findFirst({ where: { id: p.data.accountId, restaurantId: auth.ctx.restaurant.id } }); if (!account) return jsonError("Conta inválida", 400); const open = await prisma.cashSession.findFirst({ where: { restaurantId: auth.ctx.restaurant.id, accountId: p.data.accountId, status: "OPEN" } }); if (open) return jsonError("Este caixa já está aberto", 409); const row = await prisma.cashSession.create({ data: { ...p.data, openingBalance: p.data.openingBalance.toFixed(2), restaurantId: auth.ctx.restaurant.id, openedByUserId: auth.ctx.user.id } }); await audit({ restaurantId: auth.ctx.restaurant.id, actorUserId: auth.ctx.user.id, action: "OPEN", entity: "CashSession", entityId: row.id }); return NextResponse.json(decimalToNumber(row), { status: 201 }); }