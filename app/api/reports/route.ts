import { NextResponse } from "next/server";
import { prisma } from "@/backend/lib/prisma";
import { decimalToNumber, requireApiContext } from "@/backend/lib/http";
export async function GET(request: Request) { const auth = await requireApiContext(); if ("error" in auth) return auth.error; const url = new URL(request.url); const from = url.searchParams.get("from"); const to = url.searchParams.get("to"); const date = from || to ? { ...(from ? { gte: new Date(from) } : {}), ...(to ? { lte: new Date(`${to}T23:59:59`) } : {}) } : undefined; const where = { restaurantId: auth.ctx.restaurant.id, ...(date ? { occurredAt: date } : {}) }; const [transactions, categories, payables] = await Promise.all([prisma.transaction.findMany({ where, include: { category: true, account: true }, orderBy: { occurredAt: "desc" } }), prisma.category.findMany({ where: { restaurantId: auth.ctx.restaurant.id }, orderBy: { name: "asc" } }), prisma.accountsPayable.findMany({ where: { restaurantId: auth.ctx.restaurant.id }, orderBy: { dueDate: "asc" } })]); return NextResponse.json(decimalToNumber({ transactions, categories, payables })); }